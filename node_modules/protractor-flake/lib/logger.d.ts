declare class Logger {
    color: string | boolean;
    constructor(color: string | boolean);
    log(levelName: string, message: string, useColor?: boolean): void;
    protractor(message: any): void;
    colorize(message: any, useColor: boolean): any;
}
export default Logger;
