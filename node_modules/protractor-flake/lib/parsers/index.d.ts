import Parser from './parser';
declare type ParserList = {
    [key: string]: Parser;
};
declare const all: ParserList;
declare function getParser(parser?: (Parser | string)): any;
export { all, getParser };
